{
  "name": "rag-frontend",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "React chat page over the document Q&A API.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "smoke": "esbuild smoke.jsx --bundle --platform=node --format=cjs --jsx=automatic --loader:.jsx=jsx --outfile=smoke.cjs --log-level=error && node smoke.cjs"
  },
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "react-markdown": "^10.1.0",
    "remark-gfm": "^4.0.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.0.4",
    "esbuild": "^0.25.10",
    "vite": "^7.1.12"
  }
}
